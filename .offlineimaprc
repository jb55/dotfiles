[general]
accounts = main,work
pythonfile = ~/.offlineimap.py

[Account main]
autorefresh = 15
localrepository = main-local
remoterepository = main-remote
postsynchook = notmuch-update
status_backend = sqlite

[Account work]
autorefresh = 15
localrepository = work-local
remoterepository = work-remote
postsynchook = notmuch-update
status_backend = sqlite

[Repository work-local]
type = Maildir
localfolders = ~/mail/work

[Repository main-local]
type = Maildir
localfolders = ~/mail/main

[Repository work-remote]
type = Gmail
maxconnections=2
remoteuser = bill@monstercat.com
realdelete = no
idlefolders = ["[Gmail]/All Mail"]
folderfilter = filter_folder
nametrans = name_trans
remotepasseval = get_authinfo_password("imap.gmail.com", "bill@monstercat.com", 993)
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

[Repository main-remote]
type = Gmail
maxconnections=2
remoteuser = jackbox55@gmail.com
realdelete = no
idlefolders = ["[Gmail]/All Mail"]
folderfilter = filter_folder
nametrans = name_trans
remotepasseval = get_authinfo_password("imap.gmail.com", "jackbox55@gmail.com", 993)
sslcacertfile = /etc/ssl/certs/ca-certificates.crt

