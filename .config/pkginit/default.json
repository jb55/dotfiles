{
  "name": basename.replace(/^node-/, ''),
  "description": prompt(),
  "version": "0.1.0",
  "repository": {
    "url": "https://github.com/jb55/" + basename
  },
  "main": prompt('entry point', 'index.js'),
  "scripts": {
    "test": "tap test/*.js"
  },
  "dependencies": {},
  "devDependencies": {
    "tap": "~0.2.5"
  }
}
